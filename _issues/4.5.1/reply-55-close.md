@elnardosa all five items from your report are in 4.5.0, which is now public on Google Play and F-Droid.

ML-KEM-1024 and the RSA sizes read from the actual curve and modulus now, so the labels are right. Composite ML-DSA keys show all of their User IDs and you can add one. You can point the app at your own HKPS key servers in Settings and look up and publish through them. And the add-subkey screen offers post-quantum ML-KEM subkeys, with a granular keygen path where you pick each subkey by hand, including a post-quantum-only key with no classical encryption subkey.

Closing this out. If any of them does not behave the way you expect on the release build, reopen and I will take another look.

NorseHorse
